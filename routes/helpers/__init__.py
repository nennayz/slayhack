"""Compatibility-safe dashboard helper package.

The grouped modules currently re-export helper surfaces from _legacy_core so routes
can migrate imports gradually without breaking the historic routes._helpers API.
"""
